{
    "Name": "docker-registry",
    "ID": "docker-registry",
    "Address": "{{ local_ip }}",
    "Port": 5000,
    "Check": {
       "Script": "nc -w 5 -z {{ local_ip }} 5000 >/dev/null",
       "Interval": "20s"
             }
}
